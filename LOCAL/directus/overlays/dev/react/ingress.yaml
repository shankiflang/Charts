apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: react-ingress-staging
  annotations:
    kubernetes.io/ingress.class: public
    cert-manager.io/cluster-issuer: letsencrypt-prod
    kubernetes.io/tls-acme: "true"
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
spec:
  rules:
    - host: dev.test.local.slatsh.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: react
                port:
                  name: https
  tls:
    - hosts:
        - dev.test.local.slatsh.com
      secretName: dev-test-local-secret

